function diagonal(let length)
begin
	let counter = 0;
	while (counter < length)
	begin
		down 1;
		right 1;
		counter = counter + 1;
	end;
	writeln "diagonal terminada";
end;

if (2<3)
begin
	diagonal(5);
end;
